# JSON-GO

#### Description
将JSON/XML转换为golang🎈

#### Installation
1. 确保系统中安装了golang，并且版本最好>=1.13
2. `go build -ldflags "-w -s"`
3. `./main -port=:8080`
